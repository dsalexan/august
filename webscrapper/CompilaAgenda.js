var fs = require('fs');

function compila(){
    var file = fs.readFileSync('agenda.txt', 'utf8');
    var materia = {nome: '', sala:''};
    var linha = String;

    file.

    fs.appendFile('teste.txt',file);
};

compila();